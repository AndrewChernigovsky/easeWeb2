if mainPage
	div(class=logoClasses[0] + ' logo-mb')
		a(class=logoClasses[1] href="../index.html")
			+icon(
				logoIcons.viewBox,
				'50px',
				'50px',
				logoIcons.id,
			)

if anotherPage || politePage
	div(class=logoClasses[0] + ' logo-mb')
		a(class=logoClasses[1] href="../index.html")
			+icon(
				logoIcons.viewBox,
				'50px',
				'50px',
				logoIcons.id = logoIconsPath2,
			)